# Copie vers .env avant `docker compose up` local. .env est gitignored.
POSTGRES_DB=taskboard
POSTGRES_USER=taskboard
POSTGRES_PASSWORD=changeme-en-prod
DATABASE_URL=postgresql://taskboard:changeme-en-prod@db:5432/taskboard
PORT=3000
